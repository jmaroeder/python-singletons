language: python
cache:
  pip: true
  directories:
    - $HOME/.cache/pre-commit
python:
  - "3.6"
  - "3.7"
  - "3.8"
matrix:
  include:
    - python: 3.8
      env: TOXENV=precommit

install:
  - pip install tox tox-travis
script:
  - tox
